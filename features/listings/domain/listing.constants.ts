/**
 * Server-supplied marketplace constants. Centralised so every surface shows
 * identical mandated copy (MP-7).
 */

/**
 * Transaction disclaimer (MP-7, R10) — returned on every listing detail so the
 * "RideMates isn't part of the deal" copy is consistent across web and mobile.
 */
export const LISTING_DISCLAIMER =
  "RideMates isn't part of the deal — meet safely & pay directly. We never handle money or shipping.";

/** Hard cap on photos per listing (MP-1 / O2). */
export const MAX_LISTING_PHOTOS = 3;
